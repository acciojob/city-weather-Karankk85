import React from "react";
import ReactDOM from "react-dom/client";
import App from "./components/app"; // lowercase if you used `app.js`

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
